import 'package:flutter/material.dart';

class RektometerPage extends StatelessWidget {
  const RektometerPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('rektometer'),
    );
  }
}
